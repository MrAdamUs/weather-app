<!DOCTYPE html>
<html lang="en">
   {{>header}}
  <body>
   <div class="container">
       <div>
        <h1>{{title}}</h1>
       
      </div>
      
          {{>footer}}
    </div>
  </body>
</html>
